knitr::opts_chunk$set(echo = TRUE,
fig.align = "center",
fig.path = "../figures/01_DADA2")
#Efficiently load with pacman
pacman::p_load(tidyverse, dada2, phyloseq, DT, devtools, install = FALSE)
getwd()
#set the path
raw_fastqs_path <- "/data/fastq_data"
#what files are in the path?
head(list.files(raw_fastqs_path))
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "/data/fastq_data"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "data/fastq_data"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "data/fastq_data/*.fastq.gz"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "data/fastq_data/*.fastq.gz"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "/data/fastq_data/*.fastq.gz"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
raw_fastqs_path <- "data/fastq_data/*.fastq.gz"
#what files are in the path?
head(list.files(raw_fastqs_path))
#set the path
fastq_files <- Sys.glob("data/fastq_data/*.fastq.gz")
#what files are in the path?
head(list.files(raw_fastqs_path))
#what files are in the path?
head(list.files(fastq_files))
#set the path
fastq_files <- Sys.glob("data/fastq_data/*.fastq.gz")
#what files are in the path?
head(list.files(fastq_files))
